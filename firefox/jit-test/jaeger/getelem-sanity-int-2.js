var arr = ['this', 'works', 'for', 'me'];

for (var i = 0; i < arr.length; ++i) {
  var result = arr[i];

  switch (i) {
    case 0:
      'this';
      result;
      break;

    case 1:
      'works';
      result;
      break;

    case 2:
      'for';
      result;
      break;

    case 3:
      'me';
      result;
      break;
  }
}
/* int32_t getelem for dense array. */
